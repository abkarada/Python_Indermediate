# 🐍 Python Intermediate Concepts

This repository is a curated collection of **intermediate-level Python examples** designed to deepen your understanding of object-oriented programming, concurrent execution, and advanced language features.  
It is especially helpful if you're transitioning from beginner to confident developer and want to build real-world applications.

---

## 📚 Topics Covered

Each folder contains focused examples with simple, clear Python code and inline explanations.

| Folder Name                  | Concepts Included                                                                 |
|------------------------------|-----------------------------------------------------------------------------------|
| `Argument Parsing`           | Using `argparse` to handle command-line arguments dynamically                    |
| `Class And Objects`          | Understanding Python's class system, attributes, and instantiation               |
| `Client-Server`              | Basic socket programming with simple server-client communication in Python       |
| `Database`                   | Interacting with databases (e.g. SQLite), executing queries, and CRUD operations |
| `Decorators`                 | Writing and applying function and class decorators, closures, and wrappers       |
| `Encapsulation`              | Encapsulation principles and protected/private attribute usage                   |
| `Generators`                 | Efficient data handling using `yield`, generator expressions, and pipelines      |
| `Magic Methods & Dunder`     | Overloading operators and customizing class behavior with special (`__`) methods |
| `Threading and Multi Threading` | Working with `threading`, synchronization, race conditions, and GIL insights   |

---

## 🛠 Requirements

Only **Python 3.x** is required.

No external libraries are needed unless otherwise stated in a specific folder.

```bash
python3 <filename>.py
